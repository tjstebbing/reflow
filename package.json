{
  "name": "reflow",
  "version": "0.0.8",
  "description": "Simple to use workflow/transitions helper.",
  "keywords": [
    "workflow",
    "workflows"
  ],
  "scripts": {
    "test": "mocha --reporter spec --bail --check-leaks test/"
  },
  "author": "Pomke",
  "license": "MIT",
  "repository": "git@github.com:pomke/reflow.git",
  "devDependencies": {
    "mocha": "~1.21.0"
  },
  "dependencies": {
    "async": "^0.9.0"
  }
}
